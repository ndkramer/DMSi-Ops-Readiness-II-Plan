# Capability-map archive

**Deployable artifacts live only in the parent [`Capability-map/`](../) folder** (see [`README-Lambda-Deploy.md`](../README-Lambda-Deploy.md)). Nothing under `archive/` is included in the Lambda zip.

- **`legacy-2026-04-21/`** — Consolidated snapshots previously kept under duplicate paths (older `Capability-map/archive/` files and a full duplicate tree at `Capability-map/Capability-map archive/`). Historical copies only; edit the root `Capability-map/` files for production.
